meta {
  name: Create Admin
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/admin
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{SUPERADMIN_TOKEN}}
}

body:json {
  {
    "email": "leafthe78@gmail.com"
  }
}
