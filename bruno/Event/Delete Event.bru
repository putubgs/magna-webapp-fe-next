meta {
  name: Delete Event
  type: http
  seq: 5
}

delete {
  url: {{BASE_URL}}/event/{{event_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{SUPERADMIN_TOKEN}}
  Authorization: Bearer {{ADMIN_TOKEN}}
}
